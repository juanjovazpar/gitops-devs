apiVersion: apps/v1
kind: Deployment
metadata:
  name: taiga-postgresql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taiga-postgresql
  template:
    metadata:
      labels:
        app: taiga-postgresql
    spec:
      containers:
        - name: postgresql
          image: postgres:12.20
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: taiga
            - name: POSTGRES_USER
              value: taiga
            - name: POSTGRES_PASSWORD
              value: YlZBOWlCTE1lMnQzcHJSOFZtUw==
          volumeMounts:
            - name: postgresql-storage
              mountPath: /var/lib/postgresql/data
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - postgres
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
      volumes:
        - name: postgresql-storage
          persistentVolumeClaim:
            claimName: taiga-postgresql-pvc
