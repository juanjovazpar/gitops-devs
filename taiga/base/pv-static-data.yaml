apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-taiga-static-data
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 10Gi
  hostPath:
    path: /mnt/disks/taiga/static-data
  storageClassName: taiga-local-storage
