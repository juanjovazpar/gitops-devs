apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: taiga
  name: taiga-events-rabbitmq-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-storage
