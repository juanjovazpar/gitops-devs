apiVersion: v1
kind: Service
metadata:
  name: taiga-back
spec:
  type: ClusterIP
  selector:
    app: taiga-back
  ports:
    - port: 80

---
apiVersion: v1
kind: Service
metadata:
  name: taiga-front
spec:
  type: NodePort
  selector:
    app: taiga-front
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
